import type { MessageSchema } from '@typings/locale'

const message: MessageSchema = {
  'navigation-menu': {
    title: 'Editor App',
  },
  test: 'test',
  hello: 'hello',
}

export default message
